# Never commit secrets or local sbx state. NOTE: `sbx kit push` ignores this
# file, so the real protection against leaking these into a published OCI
# artifact is the clean-staging step in scripts/publish.sh.
.sbx/
.venv/
*.tar
*.zip
.DS_Store
